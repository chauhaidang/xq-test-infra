{
  "name": "todo-services-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for todo read and write services",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:verbose": "jest --verbose"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^29.5.0",
    "jest-junit": "16.0.0"
  }
}